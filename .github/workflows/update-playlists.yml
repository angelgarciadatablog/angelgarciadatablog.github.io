name: Actualizar Listas de Reproducción

on:
  schedule:
    - cron: '0 14 * * 1'  # Lunes a las 14:00 UTC (9am Perú)
  workflow_dispatch:

jobs:
  update-playlists:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Actualizar listas de reproducción
        run: |
          echo "📚 Obteniendo todas las listas de reproducción del canal..."
          node scripts/update-listas-reproduccion.js --clear-cache

      - name: Verificar cambios
        id: verify
        run: |
          if git diff --quiet datos/listas-reproduccion-playlist.json; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No hay cambios en las listas de reproducción"
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "✅ Listas de reproducción actualizadas"
          fi

      - name: Commit y Push
        if: steps.verify.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add datos/listas-reproduccion-playlist.json
          git commit -m "Chore: Actualizar listas de reproducción automáticamente [skip ci]"
          git push

      - name: Resumen
        run: |
          echo "### 📊 Resumen de Actualización" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Fecha:** $(date +'%Y-%m-%d %H:%M:%S %Z')" >> $GITHUB_STEP_SUMMARY
          echo "- **Archivo:** datos/listas-reproduccion-playlist.json" >> $GITHUB_STEP_SUMMARY
          echo "- **Cambios:** ${{ steps.verify.outputs.changed == 'true' && 'Sí' || 'No' }}" >> $GITHUB_STEP_SUMMARY
